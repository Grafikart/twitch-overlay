{
  "name": "twitch-overlay",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "gh": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "prettier": "^2.2.1",
    "vite": "^2.1.0"
  },
  "dependencies": {
    "curtainsjs": "^8.1.0"
  }
}
